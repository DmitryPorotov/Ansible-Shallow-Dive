---

- 
  hosts: all
  tasks:
    - name: Install with block 
      block:
        - name: Install patch
          package:
            name: patch
            state: present

        - name: Install python3-dnspython
          package:
            name: python3-dnspython
            state: present
      become: True


